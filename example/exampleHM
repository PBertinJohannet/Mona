data Option = Ok Int | Null;
data Point = Point Int Int;
let flip = \f a b -> f b a;
let facf = \f a -> if a == 0 then a else (f a);
let fac = fix facf;
let o = \l -> l + 1;
let numse = [\a -> a == 2, \b -> (fac b) == 20];
let hmmm = [Point 1 2, Point (fac 5) 6];
let e = let a = (\x -> x + x) in \f c -> f (a (a (a c)));
let k3 = \f b -> show ((\e -> f) b);
data In a = In a;
data Map a b = Map a b | Set Int b a;
data L a = L (Map a Int) | None;
