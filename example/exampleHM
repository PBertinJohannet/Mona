data Maybe a = Nil | Just a;
data Option a = Some (Maybe a) | None + JS Some(Just);
data Prod = P Int Char;
let ass = \(P i c) -> show i ++ [c];
let ass2 = \(Just (P i c)) (Just k) -> show i ++ [c] ++ k;
let JS = Just . P;
